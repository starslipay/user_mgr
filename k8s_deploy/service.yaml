apiVersion: v1
kind: Service
metadata:
  name: user-mgr-service
  namespace: pay-ns
  labels:
    app: user-mgr
spec:
  type: NodePort
  selector:
    app: user-mgr
  ports:
  - name: rpc
    port: 8080
    targetPort: 8080
    nodePort: 30881
    protocol: TCP
